export function generateStatReport(stat, side, matchId) {
    console.log(`Genera report per ${stat.toUpperCase()} - ${side.toUpperCase()} - match ${matchId}`);
    // Esegui fetch o genera DOM dinamico con i dati della statistica
}